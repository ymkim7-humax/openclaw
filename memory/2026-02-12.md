# 2026-02-12 Memory Log

## 빌드 자동화 및 환경 최적화
- **HP2272B 빌드 지원 시작:** 매니저님의 지침에 따라 `build_hp2272b.sh`를 생성함. (Docker `ymkim7-airoha` 컨테이너 활용, `2025q1` 기반)
- **병렬 빌드 체계 구축:** `build_parallel.sh`를 통해 HP2236B와 HP2272B를 동시에 빌드할 수 있는 구조 마련.
- **분석 최적화:** 빌드 오류 발생 시 원인 파악을 용이하게 하기 위해 두 모델 모두 `-j1` 옵션으로 빌드하도록 수정함.
- **크론 작업 통합:** 매일 오전 3시(KST)에 두 모델을 모두 빌드하는 통합 크론 작업(`67a871ea...`) 등록 완료. 기존 단독 빌드 크론은 제거함.
- **빌드 경로 격리:** 두 모델의 소스 트리가 섞이지 않도록 컨테이너 내 `hp2236b/auto_build` 및 `hp2272b/auto_build`로 경로를 엄격히 분리함.

## Gerrit 코드 리뷰 (진행 중)
- **리뷰 펜딩 현황 (총 10건):**
    - #79476: HalConfig 성능 개선 (조성욱) - **[Conditional LGTM]** Thread-safety 및 stat() 최적화 제안.
    - #79477: Reboot Schedule 추가 (최문성) - **[CRITICAL BUG]** crond 재시작 누락 및 요일(sat=7) 설정 오류 발견.
    - #79465: MediaShare 확장 (최문성) - **[LGTM]** 서비스 빈번한 재시작 부하 주의 필요.
    - #79471: PPP Interface 추가 (최문성) - **[LGTM]**.
    - #79479: Router Advertisement 추가 (최문성) - **[LGTM]** 설정값 유효성 방어 로직 우수.
- **상태 변화 알림:** 새로운 리뷰가 있거나 상태가 변경된 경우에만 DM으로 보고하도록 크론 최적화.

## [치명적] 작업 공간 유실 사고 및 복구 현황
- **사건 경위:** 오늘 오전 빌드 스크립트 오류로 `/home/ymkim7/work/airoha/hp2236b` 디렉토리(3개월치 노트 포함) 유실 확인.
- **복구 시도:** 
    - 컨테이너 내 low-level scan 시도했으나 권한 문제로 직접 수행 불가.
    - 호스트 PC용 `extundelete` 복구 가이드 및 주의사항(외부 디스크 저장 등) 매니저님께 전달.
    - **에디터 히스토리 수색:** `~/.cursor-server` 하위 캐시 데이터를 통한 텍스트 복원 작업 집중 수행 중.
- **재발 방지:** 모든 자동화 작업은 매니저님의 `work/` 디렉토리를 절대 직접 사용하지 않으며 격리된 전용 경로만 사용함.

## Gerrit 코드 리뷰 심층 분석 (업데이트)
- **#79477: Reboot Schedule (최문성) - [반려 권장/CRITICAL]**: 패치셋 4에서도 `killall crond` 이후 재시작 로직이 여전히 주석 처리되어 있음. 실행 시 시스템 스케줄러가 영구 중단되는 치명적 결함 잔존. 토요일 요일값 오류(7->6) 미수정.
- **#79476: HalConfig 성능 개선 (조성욱) - [보완 권장]**: 패치셋 2에서도 전역 컨텍스트 접근에 대한 **Thread-safety** (Mutex 등) 미확보. 멀티스레드 환경에서 Crash 위험 여전함.
- **#79486: P_CPE15 실패 수정 (Lucan) - [Merged]**: 매니저님(ymkim7)에 의해 최종 승인 및 머지 완료 확인.
- **기타 모델 확장 (#79465, #79471, #79479)**: HNI 표준 규격을 준수하고 있으며 특이사항 없음.

## [긴급] 작업 공간 유실 복구 상황
- **Cursor 히스토리 정밀 수색**: `~/.cursor-server/data/User/History` 내의 수천 개 캐시 파일을 대상으로 매니저님의 개인 노트 텍스트 조각을 추출 중. 
- **복구 범위**: 단순 소스 코드가 아닌, 매니저님의 3개월치 **수동 기록 및 판단 근거** 복원에 최우선 순위 부여.

Source: memory/2026-02-12.md
